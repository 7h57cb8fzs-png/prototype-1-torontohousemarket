name: Publish verified seller evidence
on:
  push:
    branches: [phase-6]
permissions:
  contents: read
concurrency:
  group: thm-guarded-production
  cancel-in-progress: false
jobs:
  release:
    if: contains(github.event.head_commit.message, '[seller-release]')
    runs-on: ubuntu-latest
    timeout-minutes: 12
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Validate focused seller release
        run: |
          node --check worker-v11.js
          node --check seller.js
          node --check form-inputs.js
          node --check address-input.js
          node --check app.js
          node --check admin.js
          node --test tests/seller-flow.test.mjs tests/email-copy.test.mjs tests/address-search.test.mjs tests/form-validation.test.mjs tests/address-autocomplete.test.mjs tests/listing-share.test.mjs
      - name: Publish the verified preview with rollback protection
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: 80022b7ed0560b75d96cc593b0cfaf22
          EXPECTED_BEFORE_SHA: 15af5cc85d4544abd03ea9dbbf1891ef6415cb3e74ac992ce4cba311a42540da
          CANDIDATE_VERSION_ID: 632f2362-ba9e-4f66-8dbe-e1f45c99e329
        run: node .github/scripts/promote-buyer-tools.mjs
